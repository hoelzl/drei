---
type: concept
title: GNU Emacs parity policy
description: Pinned differential scenarios, normalization rules, and divergence governance.
tags: [verification, differential, emacs, parity]
---

# GNU Emacs parity policy

GNU Emacs is a behavioral reference, not an unquestioned specification. Each
differential scenario states whether parity is required or a Drei deviation is
intentional, and every normalization is explicit.

## Pinned reference

- **Version:** GNU Emacs 29.x (validated against 29.3).
- **CI:** dedicated `parity` job on the pinned `ubuntu-24.04` runner image
  with `apt-get install -y emacs-nox` (mirrors the Recursive://Neon pattern).
  The runner pin keeps the distro Emacs at a known version; bump it only
  deliberately and re-validate all scenarios.
- **Local:** `DREI_PARITY=1 uv --no-config run pytest tests/differential -q`
  runs the same scenarios against a derived image, `drei-parity-emacs:24.04`
  — `ubuntu:24.04` with the pinned `emacs-nox` baked in, built automatically
  on first use so the apt install happens once rather than per scenario
  (`DREI_PARITY_REFRESH=1` forces a rebuild). A host `emacs` reporting the
  pinned 29.x series is used instead when present. The `GNU Emacs 29.`
  version assert pins the series regardless of image age. Without the opt-in,
  or without Docker/host Emacs, the scenarios skip — they never fabricate a
  baseline. See `docs/developer-guide/development.md` for the workflow.

## Scenario registry

| Scenario | Reference behavior | Normalization | Verdict |
| --- | --- | --- | --- |
| startup, insert, backward-char, forward-char (`tests/differential/test_emacs_parity.py`) | empty scratch-like buffer; `insert`, `backward-char`, `forward-char` | Emacs point is 1-based; Drei point is 0-based (`point_emacs - 1 == point_drei`) | parity required |
| find-file (new), insert, save-buffer (`tests/differential/test_emacs_parity.py`) | `buffer-modified-p` is `t` after `insert`, `nil` after `save-buffer`; file content on disk matches buffer text | same point normalization; Drei drives `SaveBuffer` through the production dispatch path with a fake `FilePort` and asserts content in the fake | parity required |
| kill-line ×2, yank (`tests/differential/test_emacs_parity.py`) | first `kill-line` kills text to EOL, second kills the newline, `yank` inserts the newest entry leaving point after it | same point normalization; the append chain is an intentional deviation (see below) so Drei's yank restores the full original text while batch Emacs yanks only the newest (unappended) entry | parity required on the non-append pieces (kill-to-EOL text, newline kill, yank-inserts-newest-with-point-after) |
| yank, yank-pop (`tests/differential/test_emacs_parity.py`) | `yank` inserts the newest of a 2-entry ring, then `yank-pop` (with `last-command` forced — batch cannot propagate it) replaces the yanked span with the next-older entry; entries have different lengths so point placement is pinned (`point = start + len(new)`) | same point normalization; batch `kill-line` point handling leaves point after the leftover newline, and Drei drives the same positions | parity required on the first pop; the pop cycle is an intentional deviation (see below) |
| region kill/copy (`tests/differential/test_emacs_parity.py`) | `kill-region` forward (mark behind point) and backward (point behind mark) remove point↔mark as one ring entry, point at the kill start; `copy-region-as-kill` pushes without deleting; a clean-buffer copy leaves `buffer-modified-p` nil | same point/mark normalization (1-based → 0-based); Drei clears the mark on kill/copy (batch keeps the numeric mark — deactivation is interactive) | parity required on region text/point/ring-head and the clean-copy modified flag |
| marker adjustment (`tests/differential/test_emacs_parity.py`) | kill-line before a surviving mark shifts it left; insert before shifts right; insert AT the mark keeps it before the inserted text | same normalization; Drei drives the identical command sequence | parity required on the resulting mark position |
| find-file (`tests/differential/test_emacs_parity.py`) | `find-file` on an existing file loads contents with point at start and MOD nil; a missing file yields an empty unmodified buffer with no error; a missing DIRECTORY likewise yields an empty buffer (probed twice) | same point normalization; batch minibuffer reads stdin, so only find-file semantics are compared — the prompt interaction is proven by the TermVerify scenario | parity required on all three arms |
| switch-to-buffer MRU default (`tests/differential/test_emacs_parity_windows.py`) | `(switch-to-buffer nil)` selects the most recently selected buffer that is not current; each buffer's text survives the switch | Drei's `C-x b` with empty input takes `_mru[1]` (plan 0012 D7) and drives the identical sequence through the production dispatch | parity required |
| find-file buffer reuse (`tests/differential/test_emacs_parity_windows.py`) | a second `find-file` on an already-visited path reuses the one buffer — no `notes.txt<2>` duplicate | Drei's create-or-select keys on `file_path` (plan 0012 D2) | parity required on buffer-name reuse; the `<N>` collision suffix for two *different* files sharing a basename is an intentional deviation (below), pinned by unit tests |
| window split / focus / collapse (`tests/differential/test_emacs_parity_windows.py`) | `split-window-below` copies the point into both windows, `other-window` cycles focus, each window's point survives the round-trip, `delete-other-windows` collapses to one | same point normalization (Emacs 1-based → Drei 0-based) | parity required on the observable invariants; the split-height gate (Emacs errors, Drei no-ops) is an intentional deviation (below), pinned by unit tests |

Drei's modified-flag rule is deliberately narrower than Emacs's: any
text-changing event (`TextInserted`, `TextKilled`, `TextYanked`,
`TextYankPopped`, `RegionKilled`) sets modified; a successful save
clears it; `TextUndone`/`TextRedone` DERIVE the flag by comparing the
resulting text with the buffer's last-saved text (undoing back to a
saved state clears modified — Emacs reaches the same result through its
`(t . TIMESTAMP)` undo entries — and undoing *past* a save reports
modified, because the buffer no longer matches the file). A buffer whose
file text was never observed stays modified until a save.
`SetMark`, `RegionCopied`, and `MarkExchanged`
never set it (probed: `set-mark` and `copy-region-as-kill` leave
`buffer-modified-p` nil on a clean buffer). Emacs also sets the flag on
some non-text operations; if a future scenario observes drift there,
record it as an intentional deviation with rationale.

## Intentional deviations

| Behavior | Drei | Emacs | Rationale |
| --- | --- | --- | --- |
| Append-on-consecutive-kill | consecutive `C-k` appends into one ring entry — and only *user* commands break the chain: a resize, an agent delivery, or a permission presentation landing between two kills leaves the append intact (plan 0021 D1/D2, review 0002 finding 1; `test_a_resize_between_kills_keeps_the_append_chain`, `test_delivery_does_not_break_kill_append_chain`, `test_fold_only_delivery_does_not_break_its_targets_chain`) | batch `--eval` does not append (verified against 29.3: `("\n" "ab")`, two entries; even an explicit `(setq last-command 'kill-line)` does not append in batch) | matches *interactive* Emacs `kill-line`; batch-unverifiable, so pinned by unit/property tests instead of the differential. Interactive Emacs agrees on the classification too: process output and resizes run no command, so `last-command` — and the chain — survives them |
| Empty kill at buffer end | a no-op that speaks: `Message("end-of-buffer")`, echoed `End of buffer` (`test_kill_line_at_buffer_end_is_noop`, `test_kill_line_empty_buffer_is_noop`; text pinned by `test_message_text_formats_through_the_token_table`, key-to-echo-row by `test_an_exhausted_undo_speaks_on_the_echo_row_then_clears`) | signals "End of buffer" (an error) | parity on the no-op and the words; the remaining deviation is that the message is not an *error* (mechanism row below) |
| Yank-pop cycle (second and later pops) | cursor advances modulo ring size, wrapping | batch `yank-pop` falls back to `read-from-kill-ring` (reads stdin) once `last-command` propagation ends; verified against 29.3 | batch-unverifiable, so pinned by unit/property tests (2-entry wrap) instead of the differential |
| Yank-pop without an active yank | a no-op that speaks: `Message("previous-command-not-a-yank")`, echoed `Previous command was not a yank` (`test_pop_without_active_yank_is_noop`, `test_pop_after_non_yank_command_is_noop`, `test_pop_after_keyboard_quit_is_noop`, `test_pop_after_insert_is_noop`); the message does not count as an intervening command, so a speaking pop breaks neither the undo descent nor the kill-append chain (`test_noop_pop_preserves_chain`, `test_a_speaking_no_op_does_not_break_the_undo_descent`) | signals "Previous command was not a yank" (an error) | parity on the no-op and the words; not an *error* (mechanism row below) |
| Yank-pop on a one-entry ring | silent no-op, no event | replaces the entry with itself (sets modified) | a self-replacement event would set `modified` on unchanged text, contradicting the modified-flag invariant; no-opping keeps the flag honest |
| Kill-ring capacity | fixed 60 | `kill-ring-max` (default 60, configurable) | configuration is deferred |
| Mark deactivation on edit | set mark survives insert/motion (adjusted marker-style, parity-pinned) | deactivates on many commands interactively | interactive `last-command` behavior — batch-unverifiable (batch `mark-active` survives `insert`, verified); simplest deterministic rule |
| Region kill/copy without a mark, empty region | no mark: a no-op that speaks — `Message("mark-not-set")`, echoed `The mark is not set now, or there is no region` (`test_kill_region_without_mark_is_noop`, `test_copy_region_without_mark_is_noop`); an *empty* region (mark == point) stays a silent no-op (`test_kill_region_empty_region_is_noop`, `test_copy_region_empty_is_noop`) | signals an error (no mark) / kills nothing | parity on the no-mark words, which in Emacs cover the empty region too ("or there is no region"); Drei reserves the message for the missing mark and keeps the empty-region arm silent — a deliberate split, pinned both ways. Not an *error* (mechanism row below) |
| Yank pushing the mark | yank does not touch the mark | `yank` sets the mark at the insertion start | deferred to the mark-ring slice; yank bounds stay `(start, end)` |
| Mark ring (`C-u C-@`) | absent (single mark, re-set replaces) | ring of 16 marks per buffer | deferred; single mark covers the region commands |
| `C-@`/`C-SPC` on the Windows console | undeliverable — msvcrt treats NUL as an extended-key prefix and swallows the following byte (verified live: NUL+`Z` consumes `Z`); commands reachable via harness/POSIX | real Windows Emacs uses different input plumbing (w32 events) | platform console-API constraint, not semantics; TermVerify scenario is a documented skip, byte-loop proof is in-process. The prefix+scan pair now becomes a symbolic navigation key, never the raw NUL (review 0001 finding 4: returning `"\x00"` made every arrow press run `set-mark`, so a later `C-w` killed point↔stale-mark) |
| Navigation and function keys | assembled into one symbolic key (`<up>`/`<down>`/`<right>`/`<left>`, else `<csi:…>`/`<ss3:…>`/`<ext:…>`) and left **unbound** — a silent unresolved key on both platforms (pinned by `test_editor_arrow_keys_leave_the_buffer_untouched`, `test_navigation_keys_are_unresolved_not_self_inserting`, and the TermVerify scenario `test_shipped_editor_navigation_keys_are_inert`) | arrows are bound (`previous-line`/`next-line`/`forward-char`/`backward-char`), `Home`/`End`/`PgUp` likewise | deviation: binding them is a movement-slice decision (`next-line`/`previous-line` do not exist yet), but they must never reach the buffer as input. Review 0001 finding 4: the raw bytes used to be typed into the buffer (`ESC [ A` → "[" then "A" on POSIX) |
| `M-O` (ESC O) | unreachable — `ESC O` is the SS3 sequence prefix | also unreachable in a terminal frame for the same reason (`ESC O` is read as SS3) | parity, by the same terminal constraint; `M-O` is unbound in Drei anyway |
| Bare `ESC` disambiguation | no timing: a bare `ESC` resolves only when the next character arrives, and `ESC` + non-letter dispatches the unresolved `ESC` followed by that character | `escape-time` (default 10 ms) decides between a bare `ESC` keypress and the start of a sequence | deliberate deviation: editor semantics must not depend on the clock (`AGENTS.md`), so the assembler is a pure function of the character stream. A user pressing `ESC` alone sees nothing happen until the next key |
| Undo grouping | one group per command | groups are command-loop driven; batch `undo` amalgamates across explicit `undo-boundary` calls (probed) | batch-unverifiable; one-group-per-command is the interactive equivalent |
| Nothing to undo | a no-op that speaks: `Message("no-further-undo")`, echoed `No further undo information` (`test_undo_empty_stack_is_noop`, `test_fresh_edit_after_undo_truncates_redo`) — and the descent direction is left untouched, so a held `C-/` on an exhausted history keeps no-opping (review 0001 finding 2: clearing the direction here sent the next `C-/` down the redo branch and oscillated the buffer forever; `test_exhausted_undo_does_not_flip_into_redo`) | signals "No further undo information" (probed in batch); the failed command does not turn the next undo into a redo | parity on the words and the direction; not an *error* (mechanism row below) |
| Fresh edit after undo | truncates the redo tail (later undos cannot resurrect) | keeps redo reachable — the fresh edit flips direction and the next undo REDOES the buried undo (probed: insert, undo, insert, undo → text restored, MOD t) | deliberate simplification; stock redo reachability is a follow-up slice |
| Descent gating | any semantically-acting **user** command breaks the descent; no-ops do not — including no-ops that *speak*: a `Message` describes rather than acts and never intervenes (plan 0019 D2, `test_a_speaking_no_op_does_not_break_the_undo_descent`, `test_noop_pop_preserves_chain`). Peer/housekeeping dispatches — a resize, an agent delivery or `DisplayBuffer`, a permission presentation, the turn-cancel sweep — carry semantic events but are not commands, so they never flip a descent into a redo (plan 0021 D1/D2, review 0002 finding 1: the pre-slice flip was review-0001 finding 2's class driven by terminal/peer timing; `tests/test_last_command_bookkeeping.py`, `test_delivery_does_not_break_undo_descent`) | any command flips direction (`last-command` gating), including silent no-ops; resizes and subprocess output run no command and leave it alone | batch-unverifiable interactive behavior; action-gating keeps the walk derivable from the transcript even with messages in it, and the user/peer split is Emacs's own command-loop model |
| Undo capacity | fixed 100 groups, oldest dropped silently | `undo-limit`/`undo-strong-limit` (80k/120k bytes) | configuration is deferred; mid-descent eviction shortens the deepest history only |
| Undo of the kill ring | ring untouched by undo | ring untouched (global, not buffer state) | parity — no deviation |
| Undo restoring mark/modified | mark is restored from the group; modified is DERIVED — clean exactly when the resulting text equals the buffer's last-saved text (pinned by `test_undo_to_the_saved_text_clears_modified`, `test_undo_past_the_save_keeps_the_buffer_modified`, `test_redo_forward_to_the_saved_text_clears_modified`) | markers and `(t . TIMESTAMP)` modified-flag entries are restored (probed: undo to clean state → MOD nil); Emacs counts undo-list position, not text | parity on the observable in every save-boundary case; deviation in mechanism: a buffer edited back to the saved text by ordinary commands and then undone reads clean in Drei where Emacs still reports modified. Text comparison was chosen because it makes "modified nil" mean "matches the file" (review 0001 finding 3: replaying the flag from the group reported a clean buffer after undoing past a save) |
| File line endings | detected per buffer at visit: a uniformly-CRLF file is held LF-separated in the buffer and written back as CRLF; anything else (LF, mixed, lone CR) is held and written verbatim, with stray CRs displayed as `^M`; a buffer with no file text to imitate writes LF on every platform (pinned by `tests/test_line_endings.py`) | `undecided` EOL detection: uniform CRLF decodes as `-dos` and re-encodes as CRLF; a lone LF anywhere decodes as `-unix`, leaving CRs in the buffer as `^M`; new files follow `buffer-file-coding-system`, which is host-dependent | parity on visited files, by design rather than by probe — not differential-pinned yet (scenario deferred). Deviation on new files: Drei is platform-independent by rule (AGENTS.md), Emacs is not. The port itself translates nothing (review 0001 finding 1: text-mode newline handling rewrote every saved file's endings) |
| find-file on a missing file or missing directory | empty unmodified buffer, point at start, `BufferOpened` — no error, no message | `New file` in the echo area; empty buffer, MOD nil, no error (probed) | parity on buffer state; the `New file` echo is the slice-19 mechanism's natural next emitter — deferred as follow-up work, not as debt (it is an *informational* message, not one of the eight failure/refusal rows TD-4 was paid for) |
| find-file on a directory path | `OpenFailed` (read fails with an OS error) | opens dired | dired is out of scope; recorded until a directory mode exists |
| find-file on a path whose basename is empty (a trailing slash) | refused *before* the filesystem is asked: `OpenFailed(path, "empty-basename")` echoes `<path>: empty-basename`, no buffer is created and the port is never read — the judgment is made on the slash-normalized name, so `notes/` and `C:\docs\` refuse identically on every platform (`test_trailing_slash_find_file_is_refused_before_any_read`, `test_trailing_backslash_find_file_is_refused_the_same_way`, `test_harness_trailing_slash_find_file_echoes_the_refusal`) | opens dired | deviation, same family as the row above (dired is out of scope) — but unlike a *directory*, an empty basename is refused deterministically rather than left to an OS-dependent read failure, because it names no file at all. Fixes TD-3's `""`-buffer: the old behavior created a buffer no `C-x b` input could address again (slice 20) |
| find-file with an already-open file | selects the existing buffer (create-or-select on `file_path`, no `<2>` duplicate) — **resolved by A.2** (differential `test_emacs_differential_find_file_reuses_buffer_name`) | revisits the same buffer | parity |
| find-file opening a new file | creates a new per-file buffer and selects it — the previous buffer (undo history, unsaved edits) survives — **resolved by A.2** (the slice-7 wholesale-replace/discard hazard is gone) | visits the file in a new buffer | parity on the visit; killing a modified buffer still has no guard (no kill-buffer command yet — deferred) |
| find-file with empty input | silent no-op close (no event, buffer untouched) | accepts the default (current buffer's directory) | defaults/completion are deferred; the no-op keeps the accept-always-closes invariant |
| Minibuffer abort (`C-g`) | closes the prompt, buffer and main-buffer mark untouched, never emits a quit event — and echoes `Quit` off the `MinibufferAborted` event (`test_harness_routes_minibuffer_keys` now reads the echo row; at an exit gate `test_c_g_at_the_gate_says_quit`) | `quit` — aborts the minibuffer without touching the buffer, and echoes `Quit` | parity, as of slice 19 — including the echo, whose citation was hollow until V4: the row cited `test_harness_routes_minibuffer_keys` for an echo claim the test never checked (recorded on issue #52; the test gained the assertion). The abort still emits no `KeyboardQuitEvent`: aborting a prompt is not a top-level quit and must not deactivate the main buffer's mark (mechanism row below) |
| Recursive minibuffer (`C-x C-f` while open) | ignored (silent no-op) | allowed when `enable-recursive-minibuffers` is set (default nil signals an error) | single-slot minibuffer; recursion deferred |
| Minibuffer keymap | none — control/meta keys are ignored; only printable input, RET, DEL, C-g act | full minibuffer local map (completion, history, next-line…) | completion/history deferred to a later slice |
| Minibuffer rendering of over-wide input | clipped at the frame width; cursor parks at the right edge (no horizontal scroll) | minibuffer scrolls horizontally | presentation-only deviation; scroll deferred with display refinements |
| Backspace at empty minibuffer | silent no-op | delete-backward-char fails silently at prompt start | parity on observable state |
| Buffer names / switching | buffer set with basename ids; `C-x b` prompts (empty input = MRU other buffer, parity with `(switch-to-buffer nil)` — differential `test_emacs_differential_switch_buffer_mru_default`) — **resolved by A.2** | per-file buffer names, `switch-to-buffer` | parity on switching; name collisions resolve with a `<N>` numeric suffix vs Emacs's `<dirname>` uniquify suffix (deviation: uniquify is display-naming, deferred) |
| `C-x 2` below minimum height | refused with a message — `Message("too-small-for-splitting")`, echoed `Too small for splitting` — and the frame keeps its windows (`test_split_too_small_is_a_noop`, `test_a_grown_frame_permits_a_split_the_old_size_refused`, `test_a_shrunk_frame_refuses_a_further_split`; the `DisplayBuffer` path shares the refusal but not the message — a split nobody invoked stays silent (TD-13 paid by slice 21, `test_a_frame_too_small_to_split_shows_nothing_and_breaks_nothing`)). An unknown frame size stays unconstrained and silent | `split-window-below` errors "too small for splitting" | parity on the refusal and the words; not an *error* (mechanism row below) |
| Windows | vertical stacks only (`C-x 2`/`C-x o`/`C-x 1`); per-window points (differential `test_emacs_differential_window_focus_keeps_window_points`) | horizontal splits, window trees, window configurations | deviation: one layout axis covers the transcript-beside-work case; horizontal split and window configurations deferred |
| Window point staleness across shared-buffer edits | a non-focused window's stored point/mark is **clamped** to the buffer bounds when focus returns after another window shrank the shared buffer | Emacs window points are markers that adjust on every edit (insert before window-point shifts it, delete clamps it) | deviation in mechanism, parity in the safe observable: Drei does not track marker adjustment per edit, so restore clamps. Inserts before a non-focused window's point do NOT shift it (Emacs would); pinned by the clamp tests in `test_windows.py` |
| find-file path identity | buffers are keyed by the literal path string (`./x.txt` and `x.txt` create two buffers) | Emacs compares `file-truename` (symlinks, `.`/`..`, case on case-insensitive hosts) | simplification: path canonicalization deferred with the filesystem-port work |
| kill-buffer | absent | `C-x k` kills a buffer (prompts when modified) | deferred; buffers are created and selected but never killed |
| Kill ring scope | session-global — a kill in buffer A is yankable in buffer B, and B's yank changes what A sees at its ring head (the isolation oracle in `test_interleaved_buffers_match_solo_replays` routes the other buffer's ring-touching edits to scratch precisely because the ring mediates cross-buffer interference) | global kill ring (same cross-buffer visibility) | parity — the ring is Emacs-global by design |
| Agent deliveries not undoable | `InsertAgentText` creates no undo group; `Undo` skips agent text and reaches the newest user edit (pinned by `TestAgentDeliveriesNotUndoable`) | n/a — Emacs has no agent buffer; comint output is ordinary (undoable) text | undo of an external stream is incoherent with the fold-of-effects invariant: the next delivery appends to a text the fold no longer recognizes. Hazard owned: a user who interleaves edits into the agent buffer cannot undo across an agent-delivery boundary; §A.3 (read-only regions) shrinks it by discouraging interleaved edits |
| User edits to the agent buffer not rejected | `InsertText` in the agent buffer succeeds; the next delivery appends after the edit (pinned by `TestUserEditsToAgentBufferNotRejected`) | n/a — Emacs would use `buffer-read-only` / comint field protection | §A.3 owns the enforcement mechanism (read-only/generated buffers); design 0004 D3 shipped the hook it will use — `_BufferState.kind`, already `"generated"` for every agent buffer. **Hazard owned explicitly: after a user edit, the live buffer text diverges from the pure fold of `AgentTranscriptUpdated` events — the two oracles disagree until the buffer is re-created.** Not benign: any consumer folding the transcript events sees a different text than the screen shows |
| Agent buffer is generated and visits no file | one `*agent*` buffer per ACP session (`*agent*<2>`… for further sessions), created by `CreateAgentBuffer` before any text arrives, with no `file_path`; `C-x C-s` on it reports `no-file`, and only a generated buffer accepts a delivery — naming an ordinary or missing one raises (pinned by `TestCreateAgentBuffer`, `TestOnlyGeneratedBuffersAcceptDeliveries`) | n/a for the binding; the nearest analogue is a comint buffer, which also visits no file and is conventionally named `*…*` | parity in spirit. The row exists to own two things: the `*…*` convention enters Drei here, and Drei's own default buffer is inconsistently named `scratch` rather than `*scratch*` (design 0004 D1 — renaming it would touch differential scenarios and belongs in its own change). Creation does not change focus: an agent session starting must not yank the user out of their work |
| Deliveries follow the tail, never steal point | an append moves point to the new end **only** if point was already at the old end — for the buffer's own point and for every non-focused window showing it (pinned by `TestDeliveriesFollowTheTail`, `test_point_follows_the_tail_only_from_the_tail`) | comint moves point for output only when point was at the process mark | parity in shape, stated over *windows* because A.2 made window point distinct from `BufferValue.point`: a user scrolled back through the transcript in one window is not dragged to the end because output arrived in another window on the same buffer |
| Agent file edits rendered, not applied | `diff` tool-call content lands in the transcript as text (path + old/new); no target buffer is modified and the filesystem port is not involved (pinned by `test_diff_renders_old_and_new_verbatim` and the §B.7 golden trace) | n/a — no Emacs equivalent; ACP peers with buffer support apply edits | applying diffs requires multiple buffers (§A.2) and a conflict policy for modified user buffers — a separate slice. B.6 refuses `fs/*` requests (Drei advertises no fs capability), so diffs only ever arrive as transcript content |
| External deliveries bypass the minibuffer gate | `DeliverProcessOutput` / `DeliverSessionEffects` / `InsertAgentText` act while the minibuffer is open; every other non-minibuffer command is a silent no-op (pinned by `TestMinibufferDoesNotSwallowDeliveries`). `PromptPermission` (B.8) is delivery-class too: a permission request arriving while a prompt is open **queues** and is presented after the prompt resolves (pinned by `test_is_gate_exempt_like_a_delivery`, `test_request_during_text_prompt_presented_after`) | n/a — Emacs has no delivery commands; async process output inserts regardless of minibuffer state | a dropped delivery would desync the agent-buffer fold from the transcript (the fold advances but the event is lost) — deliveries are not user input and must not be gated on it. A swallowed permission request is worse: the agent blocks indefinitely awaiting its answer, so queue-and-present replaces drop |
| `allow_always` honoured only within the ACP session | the auto-approval cache is cleared on `new_session()`; Drei has no cross-session persistence (pinned by `test_cache_resets_on_new_session`) | n/a — no Emacs equivalent; ACP `allow_always` implies durable approval | design 0003's session-persistence open question is unresolved; honouring `always` across restarts would require a store Drei does not have. Hazard owned: a user who chose "always" is re-prompted next session — safe (fail-closed), recorded here until persistence lands |
| Auto-approval scoped to tool identity + arguments, never the per-call `toolCallId` | the identity key is `toolCall.kind`/`title` + canonical-JSON of the params (including arguments and options) with the volatile per-call fields — top-level `sessionId` and `toolCall.toolCallId` — **stripped**; any other change re-prompts (pinned by `test_fresh_tool_call_id_same_arguments_auto_approves`, `test_different_arguments_re_prompts`, `test_different_title_re_prompts`, `test_changed_options_re_prompts`) | n/a — no Emacs equivalent | ACP `allow_session`/`allow_always` are scope grants over a *tool*; keying on the per-invocation `toolCallId` would both miss the cache and fail open if an agent reused an id. Fail-closed: changed arguments/options always re-prompt rather than inherit a grant. Review 0001 finding 6: the key used to canonicalize the *full* params, `toolCallId` included, so a conforming agent (fresh id per call) never hit the cache and the auto-answer branch was dead code |
| Malformed permission payloads never auto-approve | option kinds matched by explicit 0.9.0 enum, never `startswith`; duplicate `optionId`s resolve to the LAST (shadowing) match; a cached identity offering no allow option re-prompts (pinned by `test_invented_allow_kind_*`, `test_duplicate_option_id_last_match_wins_no_cache_poison`, `test_select_auto_option_prefers_scope_then_once`) | n/a — no Emacs equivalent | a hostile agent can name an option `allow_evil` or order a duplicate `allow_always` before a reject; startswith/first-match would auto-approve it. Fail-closed: anything not exactly an enum kind, or ambiguous, re-prompts the human |
| `RET` at a permission prompt grants the narrowest scope | accept selects the first usable `allow_once` option and otherwise resolves `Cancelled` — never a broader scope, whatever order the agent listed (pinned by `test_accept_selects_allow_once_even_when_listed_last`, `test_accept_never_grants_broader_scope`, `test_accept_without_allow_option_maps_to_cancelled`) | n/a — no Emacs equivalent; `y-or-n-p` has no scope dimension | the option list is agent-controlled, so "first allow option" (review 0001 finding 9) let an agent list `allow_always` first and turn a habitual RET into a permanent session-wide grant. Broader scopes require their explicit `s`/`a` keys — the human must name the scope they are granting |
| Non-string `optionId`s are unselectable | an option whose `optionId` is not a string is skipped at every selection site — key press, RET, and auto-approval (pinned by `test_non_string_option_id_is_not_selectable`, `test_key_to_decision_ignores_option_without_option_id`, `test_accept_skips_allow_option_without_id`) | n/a — no Emacs equivalent | ACP `optionId`s are strings; `str()`-coercing `1` into `"1"` (review 0001 finding 25) answers with a value the agent never sent, which a strict peer will not match. Fail-closed: an unusable option cannot be chosen |
| Turn cancellation answers every pending permission | `cancel()` answers each entry in `in_flight_incoming` with the `cancelled` outcome, and the session's `AbortPendingPermissions` closes an open *choice* prompt and drains the queue — text prompts are untouched (pinned by `test_cancel_sweeps_pending_permission_requests`, `test_sweep_closes_choice_prompt_and_drains_queue_without_deciding`, `test_sweep_leaves_text_prompt_open`) | n/a — no Emacs equivalent | ACP 0.9.0 requires it: a request left pending hangs the agent, and a queued prompt presented after the turn died asks the human about work that no longer exists (review 0001 finding 10). Wired by slice 20: the pump reads `KeyboardQuitEvent` out of the command outcome (design 0005 D7's seam) and drives `cancel()` under `PROMPT_IN_FLIGHT` — the `C-g`-cancels-turn row below |
| A permission answer is a user command and intervenes | answering or aborting a permission prompt is a minibuffer command, so it breaks the undo descent and kill-append chain the user had open — while the peer's *presentation* of the same prompt intervenes in nothing (the Descent-gating row's classification; `test_the_users_answer_still_intervenes`, `test_a_permission_presentation_and_its_abort_do_not_flip_the_descent`) | `y-or-n-p` reads its key inside the prompting command, so `last-command` survives an answer: a kill chain or undo descent continues across an interposed prompt | deviation, owner-resolved (plan 0021 Q1, option b): Drei's prompt answers ARE commands — the established minibuffer model — and re-scoping them as non-intervening is minibuffer semantics, not bookkeeping gating. Recorded so the difference is owned rather than discovered |
| Save with no file: `no-file` | a pathless buffer's `C-x C-s` emits `SaveFailed(<buffer name>, "no-file")`, echoing `scratch: no-file` (pinned by `test_save_without_file_path_names_buffer_with_no_file_token`) | `save-buffer` on a buffer with no file prompts for a filename (`write-file`) | deviation: Drei has no write-file prompt yet, so the save reports why it could not happen. Review 0001 finding 26: the event used to hardcode the name `"scratch"` and the token `not-found`, describing a missing file rather than the absence of one |
| Shrinking below the split minimum keeps every window | `ResizeFrame` updates the frame size and nothing else; the renderer draws what fits, so growing the frame back restores both panes with their points and marks (pinned by `test_shrinking_below_the_split_minimum_keeps_every_window`, `test_growing_back_restores_the_layout_with_points_intact`, `test_shrink_below_the_split_minimum_degrades_in_stages`, `test_growing_the_frame_back_restores_both_panes`) | deletes the windows that no longer fit, permanently — growing the frame back does not bring them back | deviation (plan 0015 D7): a resize is an accidental, frequent gesture in a way `C-x 0` is not, so it must not be destructive. Hazard owned: while shrunk, a window can exist that the frame does not render but `C-x o` still reaches (pinned by `test_focus_still_cycles_into_a_window_the_frame_cannot_show`) |
| A resize is observed on the input queue, not by a signal | the size watcher polls, so a resize is observed within one poll interval rather than at the instant it happens, and **only when the size actually changed** — a resize to the current geometry produces no event at all (the polling itself is exercised by `test_threaded_source_merges_keys_and_sizes_onto_one_queue`, which pins that an unchanged poll queues nothing; the end-to-end reflow on a real ConPTY by `test_shipped_editor_resize_scenario`) | reflows on `SIGWINCH`, immediately | deviation (plan 0015 D4): `SIGWINCH` and `WINDOW_BUFFER_SIZE_EVENT` share no shape across POSIX and the Windows console, and growing two platform paths is not this slice's subject. Presentation-only lag; the semantic frame size is correct from the next command onward. Consequence for verification: because the watcher filters unchanged sizes, a TermVerify epoch that dispatches a resize to the geometry the terminal already has receives no marker and dies on the abort deadline — a resize scenario must genuinely change the geometry |
| A frame too short for its panes drops the echo row first | row truncation cuts from the bottom, so a two-row split frame renders two modelines and no echo row (pinned by `test_shrink_below_the_split_minimum_degrades_in_stages`) | gives up windows before the echo area | **unintended** deviation, recorded as TD-10 rather than fixed in plan 0015: rendering priority is its own decision needing its own row. The current behavior is pinned so the fix surfaces as a failing test |
| An agent transcript is shown in another window, never the focused one | `DisplayBuffer` splits once if the frame holds a single window and the split gate permits, then replaces the window *after* the focused one; focus never moves, the new view starts at the top of the buffer, and a frame too small to split shows nothing at all (pinned by `test_a_single_window_frame_splits_to_make_room`, `test_focus_never_moves`, `test_an_existing_second_window_is_reused_rather_than_a_third_created`, `test_a_frame_too_small_to_split_shows_nothing_and_breaks_nothing`) | `display-buffer` consults `display-buffer-alist`, window parameters and `pop-to-buffer` policy, and can select the window | deviation (plan 0016 D-display): Drei has no display policy mechanism, and the one rule that matters is design 0004 D1's — agent output appearing must not take the user out of what they were doing. Reusing a window rather than subdividing further is Emacs's own instinct; a frame that grew a window per agent session would shrink to nothing |
| An agent delivery redraws without a readiness marker | agent output reaches the frame with no `OSC 7791;ready` after it, because it belongs to no input epoch (pinned by `test_an_agent_redraw_carries_no_readiness_marker`; the consequence is exercised by `test_shipped_editor_runs_an_agent_turn`) | n/a — no Emacs equivalent; this is a TermVerify cooperation rule, not an editor behavior | deliberate and load-bearing (design 0005): a marker the verifier did not dispatch an input for would be counted against the *next* keystroke and shift every epoch after it. The cost is that the end-to-end agent scenario waits on frame content with a wall-clock deadline — the only such wait in the suite. The fix belongs upstream: a second marker kind for non-input-driven quiescence, filed with TermVerify, never a Drei-private marker |
| `C-c a` starts an agent turn | `C-c` is a prefix of its own and `C-c a` opens a text prompt whose accepted text becomes an ACP `session/prompt`; the child is spawned lazily on that first use (pinned by `test_c_c_a_resolves_to_the_agent_prompt`, `test_accepting_records_what_the_user_asked_for`, `test_shipped_editor_runs_an_agent_turn`) | `C-c` is reserved for the user and the major mode; Emacs binds nothing there globally | not a deviation — this is the prefix Emacs reserves for exactly this, and `C-x` stays the global prefix. Recorded because it is the first binding Drei has added outside `C-x`, and because ETX reaching the editor at all depends on raw mode having cleared `ISIG` / `ENABLE_PROCESSED_INPUT` |
| An agent that will not start leaves an editor, not a traceback | a failed spawn writes the normalized token (`not-found`, `permission-denied`, …) to the `*agent-log*` buffer and the editor carries on; the child's stderr streams to the same buffer (pinned by `test_a_launch_failure_is_a_normalized_token_not_a_traceback`, `test_shipped_editor_survives_an_agent_that_will_not_start`) | n/a — no Emacs equivalent | deliberate: `drei file.txt` on a machine with no agent installed must be exactly the editor it was. The buffer rather than the echo area because stderr is a *stream*, not a message — a transient echo would lose a launch failure the user needs to read |
| `C-g` is `keyboard-quit` | top-level `C-g` deactivates the mark (`test_keyboard_quit_clears_mark`), echoes `Quit` and leaves the editor running (`test_c_g_does_not_end_the_run`, `test_editor_esc_consumed_as_chord_start_then_keyboard_quit`), aborts a minibuffer prompt without touching the main buffer's mark (`test_harness_routes_minibuffer_keys`), and — with an agent turn in flight — cancels the turn (the next row) | `keyboard-quit`: aborts the current operation, deactivates the mark, signals `quit` | parity, as of slice 17. It was **not** parity for sixteen slices: `C-g` ended the run and discarded every modified buffer, wiring the reference editor's safest key to its most destructive outcome, and the deviation went unregistered the whole time |
| `C-g` with an agent turn in flight cancels the turn | the keyboard-quit meaning runs first (mark off, `Quit`); then the pump — reading `KeyboardQuitEvent` out of the command outcome, design 0005 D7's seam — writes `session/cancel`, answers every pending permission request `cancelled`, and clears the presentation, in that order (design 0005 D5); the transcript shows `── end turn (cancelled) ──` when the agent answers, as ACP 0.9.0 requires, and a repeated `C-g` before the answer re-sends the idempotent notification rather than raising. One `C-g` peels one layer: at a *permission prompt* `C-g` is the shipped deny, so cancelling the turn is the second `C-g`; at an *exit prompt* it abandons the exit, leaving the turn in flight; at a *text prompt* it aborts the prompt alone and the turn waits for the next `C-g` (`test_c_g_at_a_text_prompt_mid_turn_aborts_only_the_prompt`); a *pending prefix* peels *with* the turn — `C-x C-g` resolves to the same `KeyboardQuit()` as a bare `C-g`, the event carries no prefix provenance, and the pump does not distinguish, Emacs's own `C-g`-from-a-prefix instinct (`test_c_g_with_a_pending_prefix_cancels_prefix_and_turn_together`); with no agent, during the handshake, or idle it is a plain keyboard-quit (`tests/test_pump.py` — `TestTurnCancellation`, nine pins including the wire order and the phase guards; `test_c_g_cancels_a_held_turn_and_the_next_prompt_still_works` over a real child; `test_shipped_editor_cancels_a_held_turn_with_c_g` off the shipped frame) | n/a — Emacs has no agent integration; `keyboard-quit` aborts the current operation | n/a (the keyboard-quit half is parity — the row above). Deliberate design, slice 20 / TD-2 paid: the trigger is the key Emacs users already press when something runs too long. The session holds no `AcpMachine`; the phase guard keeps `C-g` outside `PROMPT_IN_FLIGHT` free of protocol meaning |
| `C-x C-c` offers to save each modified buffer | with nothing modified the run ends immediately (`test_editor_writes_readiness_and_exits_on_c_x_c_c`, `test_nothing_modified_exits_without_asking`); a modified file-visiting buffer is offered `Save file <path>? (y or n) ` per buffer, and `y` writes it and ends the run with `Wrote <path>` on the final frame (`test_c_x_c_c_offers_to_save_a_modified_buffer`, `test_shipped_editor_kill_yank_scenario`) | `save-buffers-kill-terminal` prompts for each modified buffer before exiting | parity, as of slice 18 (TD-11 paid). It was **not** parity for seventeen slices: exiting discarded every modified buffer in silence. The remaining differences are the seven rows below — the answer set, the final gate's shape, which buffers are counted, the ordering, the unrecognized-key echo, a queued permission request, and what a failed write says |
| The exit answer set is `y` / `n` / `C-g` | `y` saves (stage 1) or exits (stage 2), `n` skips or refuses, `C-g` abandons the whole exit and leaves already-saved buffers saved; every other *answer* key — `RET`, `DEL`, `SPC`, `Y`, `N` included — does not answer: it is met with `Please answer y or n` riding the standing prompt, and the prompt stays up (`test_an_unrecognized_key_leaves_the_offer_standing`, `test_del_at_an_exit_prompt_leaves_it_standing`, `test_c_g_leaves_already_saved_buffers_saved`) | `save-some-buffers` runs through `map-y-or-n-p`, so besides `y`/`n` it accepts `SPC` (yes) and `DEL` (no) from `query-replace-map`, plus `!` (save all remaining), `.` (save this one and exit), `q` (stop asking), `C-r` (view the buffer), `C-f` and `C-h` | deviation, deferred by plan 0018 D5/§4: the extras are convenience on a list that is one or two entries long in practice, and `C-r` needs a recursive view Drei has no machinery for. `C-x s` — the other way into Emacs's answer set — does not exist either, and a later slice can add both together. The deviation fails **safe** in both directions: a key Emacs binds to an answer does nothing here rather than answering wrongly (slice 18's review widened this row, which previously omitted `SPC`/`DEL` entirely — review 0002 finding 15 maps the citation) |
| The final exit gate is a single-key `y`/`n` | `Modified buffers exist; exit anyway? (y or n) ` resolves on one key, and `RET` is **not** a default yes (`test_ret_is_not_a_default_yes_at_the_gate`) | `save-buffers-kill-emacs` uses `yes-or-no-p` — the user types `yes` and `RET` | deviation: Drei has no text-confirm mode, and adding a fourth minibuffer mode for one question is worse than the friction it buys. `RET` answering nothing is the part that matters, and it is pinned — the same reasoning as the approval bridge's finding 9, where a habitual `RET` must not resolve a question about losing work |
| A modified pathless buffer blocks the exit but is never offered | a buffer with no `file_path` goes straight to the gate and must be confirmed; it is never offered a save (`test_a_modified_pathless_buffer_goes_straight_to_the_gate`, `test_n_at_the_gate_keeps_the_pathless_text`) | `save-buffers-kill-emacs` counts only `(and (buffer-file-name) (buffer-modified-p))`, so a modified `*scratch*` exits in silence | deviation, deliberate (plan 0018 D2, chosen by the owner over Emacs parity). It links the `no-file` row: Drei has no `write-file`, so such a buffer cannot be saved *at all*, and counting it is the only protection available. The startup buffer is where a new user's first keystrokes land |
| Buffers are offered in creation order | the sequence walks `EditorSession.buffers`, oldest first (`test_each_modified_file_buffer_is_offered_in_creation_order`) | `save-some-buffers` walks `buffer-list`, which is recency-ordered | deviation: deterministic, and already the order the session exposes. Recency would make the prompt sequence depend on focus history, so the same two edits could be offered in either order |
| An unrecognized key at an exit prompt speaks | the prompt stays up and the message rides it as a suffix — `Message("answer-y-or-n")`, rendered `Please answer y or n` (`test_an_unrecognized_key_leaves_the_offer_standing`, `test_del_at_an_exit_prompt_leaves_it_standing`, `test_ret_is_not_a_default_yes`, `test_ret_is_not_a_default_yes_at_the_gate`; on the frame: `test_an_unrecognized_key_rides_the_standing_prompt`) | echoes `Please answer y or n` | parity on the words. Where Emacs shows the message and restores the prompt on a redisplay timer, Drei appends it to the standing prompt (mechanism row below) |
| A save that fails during the exit reports on the next prompt, and the exit continues | `y` on a file the port refuses opens the next prompt with `[<path>: <token>]` appended — `_echo_for`'s own `SaveFailed` shape — and the sequence carries on to the next buffer or to the gate (`test_a_failed_save_names_itself_in_the_prompt_that_follows`, `test_a_failed_save_names_itself_before_the_next_offer`, `test_a_failed_save_at_the_exit_prompt_is_readable_on_the_frame`, `test_the_note_does_not_outlive_the_prompt_it_was_raised_on`). The note is a **suffix**: the echo row is hard-clipped and the shipped scenarios run at 40 columns, so one half is sacrificed and it must not be the question (`test_the_question_survives_clipping_when_a_note_is_present`, `test_the_offer_keeps_its_filename_when_a_note_is_present`, `test_the_exit_question_survives_a_narrow_frame_carrying_a_failure`) | **not probed.** A refused write raises `file-error` out of `basic-save-buffer`, which would unwind `map-y-or-n-p` and abort `save-buffers-kill-terminal` outright — so Emacs plausibly stops the whole exit rather than continuing to the next buffer. That is a reading of the source, not an observation | deviation, and the reference behaviour is **deliberately marked unprobed rather than guessed** (slice 19's round-2 review — this row previously claimed "parity in effect" on the strength of three Drei-side tests and no Emacs at all; review 0002 finding 15 maps the citation). Drei's own behaviour is deliberate: a failed write must not read as a save, and the exit continues because stage 2 re-derives what is at risk from the buffers, so a failure cannot be lost. Settling the reference side needs a differential scenario with a read-only fixture; until one exists, treat this row as describing Drei only |
| A permission request queued behind an exit prompt is presented only if the exit is abandoned | the request queues while the prompt is open, is presented after `C-g` or an `n` at the gate, and is dropped when the exit completes (`test_a_request_arriving_behind_an_exit_prompt_queues`, `test_c_g_abandoning_the_exit_presents_the_queued_request`, `test_a_completed_exit_does_not_present_the_queued_request`) | n/a — no Emacs equivalent | deliberate (plan 0018 D7), and recorded next to the turn-cancellation row for the same reason: a request left pending hangs the agent for the rest of the run, while one presented after the process decided to die asks about work that is about to stop existing. `AbortPendingPermissions` mid-sequence clears the queue and leaves the exit prompt standing — an exit prompt is not a choice prompt (`test_abort_pending_permissions_leaves_the_exit_prompt_standing`) |
| `C-g` after a prefix cancels it and quits | `C-x C-g` resolves to `keyboard-quit`: the pending prefix is dropped, the mark deactivated, `Quit` echoed (pinned by `test_c_g_cancels_a_pending_prefix_and_quits`, `test_c_g_after_a_prefix_cancels_it_and_quits`) | `C-g` cancels the prefix and signals `quit` | parity, as of slice 17 (TD-5 paid). It was a silent no-op before: one `UnresolvedKey("C-x C-g")`, no quit, no echo, and the mark survived |
| Every other unbound key after a prefix speaks | `C-x z` resolves to one `UnresolvedKey` (`test_a_prefix_leaves_every_other_unbound_key_unresolved`), recorded under the composed name — and echoes `C-x z is undefined`, composed in the harness because no command reaches the session for it to speak about (plan 0019 D7; `test_harness_records_unresolved_keys` reads the echo row) | echoes `C-x z is undefined` in the echo area | parity on the words. Only `C-g` is special-cased, because it is the key whose entire job is getting out of a half-typed chord |
| Echo messages (the slice-19 mechanism) | no-ops and refusals speak via `Message(token, subject)` events recorded in the transcript; the session owns the Drei-token vocabulary, the harness owns the single token→English table and the one `_echo_for` derivation site; a message lives until the next command, and one raised while a prompt is open rides that prompt as a `[suffix]` rather than owning the echo row (D3/D6) | `message` echoes in the echo area and logs to `*Messages*`; a message during an active minibuffer is shown and the prompt restored by redisplay | deviations, deliberate (plan 0019 §6): the English is Drei's own — tokens are pinned, prose is not byte-compared, and the differential tier never compares echo text. There is no `*Messages*` log and no redisplay timer; the note rides the prompt until the next key |
| Errors do not signal | an empty kill, a no-mark region kill, an exhausted undo, a refused split say their sentence and carry on | the same cases raise real errors — aborting a keyboard macro, entering the debugger under `debug-on-error` | deviation, deliberate (plan 0019 §6.4): Drei has no macros, no condition system, and no debugger, so "signal" would mean nothing but the message |
| `C-g` at a prompt echoes `Quit` without a quit event | the echo derives from `MinibufferAborted`, never from `KeyboardQuitEvent` — the main buffer's mark survives a prompt abort (row 92, `test_harness_routes_minibuffer_keys`) | `quit` signals | deliberate (plan 0019 §6.5, slice 17's separation stands): aborting a prompt is not a top-level quit, and unifying the two echo paths would re-couple prompt abort to mark deactivation |

## Rules

1. A scenario compares **semantic** observations (text, point) only. Terminal
   presentation differences are out of scope for parity.
2. Normalization rules are part of the scenario and change only with a
   readable diff and explicit review.
3. Unexpected differences fail; intentional Drei deviations are recorded in
   this registry with a rationale, never silenced in test code.
4. Every deviation row names the tests that pin it, in backticks.
   `tests/test_parity_registry.py` fails if a cited test does not exist, so a
   row cannot go on claiming evidence that was renamed or deleted. The
   converse — a deviation shipped with no row — is not mechanically
   checkable and remains a review responsibility (review 0001 finding 16).
